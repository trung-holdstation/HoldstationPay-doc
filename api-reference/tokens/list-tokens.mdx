---
title: List Tokens
openapi: GET /api/v1/tokens
---

<ResponseField name="data" type="object">
  <Expandable title="data">
    <ResponseField name="tokens" type="Token[]">
      <Expandable title="Token">
        <ResponseField name="id" type="string">Unique token UUID</ResponseField>
        <ResponseField name="name" type="string">Token full name (e.g. `Tether USD`)</ResponseField>
        <ResponseField name="symbol" type="string">Token symbol (e.g. `USDT`)</ResponseField>
        <ResponseField name="name_override" type="string">Display name including chain (e.g. `Tether USD (BSC)`)</ResponseField>
        <ResponseField name="address" type="string">Token contract address on-chain</ResponseField>
        <ResponseField name="chain_id" type="integer">Blockchain network ID</ResponseField>
        <ResponseField name="decimals" type="integer">Number of decimal places for the token</ResponseField>
        <ResponseField name="price" type="string">Current token price in USD</ResponseField>
        <ResponseField name="price_change_24h" type="string">24-hour price change percentage</ResponseField>
        <ResponseField name="image" type="string">URL to the token logo image</ResponseField>
        <ResponseField name="active" type="boolean">Whether the token is currently active for trading</ResponseField>
        <ResponseField name="withdrawal_contract" type="string">Withdrawal contract address (zero address if not applicable)</ResponseField>
        <ResponseField name="created_at" type="Timestamp">Record creation time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="updated_at" type="Timestamp">Record last updated time — `{ seconds, nanos }`</ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="page" type="integer">Current page number</ResponseField>
    <ResponseField name="page_size" type="integer">Number of items per page</ResponseField>
    <ResponseField name="total_count" type="integer">Total number of tokens available</ResponseField>
  </Expandable>
</ResponseField>
