---
title: Verify Bank Account
openapi: POST /api/v1/payment-methods/verify
---

<ResponseField name="data" type="object">
  <Expandable title="data">
    <ResponseField name="bank" type="object">
      <Expandable title="Bank">
        <ResponseField name="id" type="integer">Unique bank ID</ResponseField>
        <ResponseField name="bank_name" type="string">Full bank name</ResponseField>
        <ResponseField name="short_name" type="string">Bank short name / abbreviation</ResponseField>
        <ResponseField name="bin_code" type="string">Bank Identification Number (BIN) code</ResponseField>
        <ResponseField name="image" type="string">URL to the bank logo image</ResponseField>
        <ResponseField name="created_at" type="Timestamp">Record creation time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="updated_at" type="Timestamp">Record last updated time — `{ seconds, nanos }`</ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="account_name" type="string">Account holder's full name</ResponseField>
    <ResponseField name="account_number" type="string">Verified bank account number</ResponseField>
  </Expandable>
</ResponseField>
