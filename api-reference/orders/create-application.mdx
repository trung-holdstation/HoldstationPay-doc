---
title: Create Wholesale Application
openapi: POST /api/v1/wholesale/applications
description: Submits a wholesale application for the authenticated partner. Wholesale partners receive custom exchange rates.
---

<ResponseField name="data" type="object">
  <Expandable title="data">
    <ResponseField name="application" type="object">
      <Expandable title="WholesaleApplication">
        <ResponseField name="id" type="string">Unique application UUID</ResponseField>
        <ResponseField name="user_id" type="string">Partner identifier who submitted the application</ResponseField>
        <ResponseField name="status" type="integer">`0`=Pending, `1`=Approved, `2`=Rejected</ResponseField>
        <ResponseField name="default_rate" type="number">Requested default wholesale rate</ResponseField>
        <ResponseField name="submitted_at" type="Timestamp">Submission time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="reviewed_at" type="Timestamp">Review time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="updated_at" type="Timestamp">Last update time — `{ seconds, nanos }`</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
