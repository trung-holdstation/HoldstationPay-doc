---
title: Get Order by Reference
openapi: GET /partners/orders/reference-no/{reference_no}
---

<ResponseField name="data" type="object">
  <Expandable title="data">
    <ResponseField name="orders" type="Order[]">
      <Expandable title="Order">
        <ResponseField name="id" type="string">Unique order UUID</ResponseField>
        <ResponseField name="code" type="integer">Internal status code from payment provider</ResponseField>
        <ResponseField name="order_type" type="integer">`1`=Deposit, `2`=Withdrawal</ResponseField>
        <ResponseField name="state" type="integer">`1`=New, `2`=Processing, `3`=Completed, `4`=Failed, `5`=Closed</ResponseField>
        <ResponseField name="processing_state" type="integer">`10`–`16` for deposit, `20`–`25` for withdrawal — see [List Orders](/api-reference/orders/list-orders)</ResponseField>
        <ResponseField name="amount" type="string">Input amount</ResponseField>
        <ResponseField name="outcome" type="string">Amount the user receives after fees</ResponseField>
        <ResponseField name="currency" type="string">`vnd` or `usd`</ResponseField>
        <ResponseField name="rate" type="string">Exchange rate applied</ResponseField>
        <ResponseField name="original_rate" type="string">Exchange rate before buffer adjustment</ResponseField>
        <ResponseField name="token_address" type="string">Token contract address</ResponseField>
        <ResponseField name="chain_id" type="integer">Blockchain network ID</ResponseField>
        <ResponseField name="recipient" type="string">Recipient wallet address (deposit) or HPay wallet to send tokens to (withdrawal)</ResponseField>
        <ResponseField name="reference_no" type="string">Partner's reference number</ResponseField>
        <ResponseField name="external_id" type="string">External order ID from payment provider</ResponseField>
        <ResponseField name="partner_id" type="string">Partner identifier</ResponseField>
        <ResponseField name="user_id" type="string">User identifier</ResponseField>
        <ResponseField name="provider" type="integer">Payment provider identifier</ResponseField>
        <ResponseField name="processing_time" type="integer">Time taken to process in milliseconds</ResponseField>
        <ResponseField name="client_ip" type="string">IP address of the client that created the order</ResponseField>
        <ResponseField name="callback" type="string">Webhook callback URL</ResponseField>
        <ResponseField name="total_fee_vnd" type="number">Total fee in VND</ResponseField>
        <ResponseField name="fees" type="object">`{ service_fee, min_fee }` — fee breakdown in VND</ResponseField>
        <ResponseField name="pay_data" type="object">Bank payment details (deposit only)</ResponseField>
        <ResponseField name="payment_info" type="object">Bank account for VND payout (withdrawal only)</ResponseField>
        <ResponseField name="error" type="object">Error details if failed</ResponseField>
        <ResponseField name="created_at" type="Timestamp">Order creation time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="updated_at" type="Timestamp">Last update time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="expired_at" type="Timestamp">Order expiry time — `{ seconds, nanos }`</ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="total_count" type="integer">Total number of matching orders</ResponseField>
  </Expandable>
</ResponseField>
