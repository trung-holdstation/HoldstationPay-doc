---
title: Create Withdrawal
openapi: POST /partners/orders/withdrawal
description: Creates a new offramp order for a user to sell tokens and receive VND via bank transfer. See [Offramp Flow](/guides/offramp-flow) for the full integration guide.
---

<ResponseField name="data" type="object">
  <Expandable title="data">
    <ResponseField name="order" type="object">
      <Expandable title="Order">
        <ResponseField name="id" type="string">Unique order UUID</ResponseField>
        <ResponseField name="code" type="integer">Internal status code from payment provider</ResponseField>
        <ResponseField name="order_type" type="integer">`2` = Withdrawal</ResponseField>
        <ResponseField name="state" type="integer">`1`=New, `2`=Processing, `3`=Completed, `4`=Failed, `5`=Closed</ResponseField>
        <ResponseField name="processing_state" type="integer">`20`=CryptoPending, `21`=CryptoConfirmed, `22`=CryptoFailed, `23`=FiatPending, `24`=FiatConfirmed, `25`=FiatFailed</ResponseField>
        <ResponseField name="amount" type="string">Input amount in USD</ResponseField>
        <ResponseField name="outcome" type="string">VND amount the user will receive after fees</ResponseField>
        <ResponseField name="currency" type="string">Currency of the amount (`vnd` or `usd`)</ResponseField>
        <ResponseField name="rate" type="string">Exchange rate applied to this order</ResponseField>
        <ResponseField name="original_rate" type="string">Exchange rate before buffer adjustment</ResponseField>
        <ResponseField name="token_address" type="string">Token contract address</ResponseField>
        <ResponseField name="chain_id" type="integer">Blockchain network ID</ResponseField>
        <ResponseField name="recipient" type="string">Holdstation Pay's wallet address the user must send tokens to</ResponseField>
        <ResponseField name="reference_no" type="string">Partner's reference number</ResponseField>
        <ResponseField name="external_id" type="string">External order ID from payment provider</ResponseField>
        <ResponseField name="partner_id" type="string">Partner identifier</ResponseField>
        <ResponseField name="user_id" type="string">User identifier</ResponseField>
        <ResponseField name="provider" type="integer">Payment provider identifier</ResponseField>
        <ResponseField name="processing_time" type="integer">Time taken to process in milliseconds</ResponseField>
        <ResponseField name="client_ip" type="string">IP address of the client that created the order</ResponseField>
        <ResponseField name="callback" type="string">Webhook callback URL</ResponseField>
        <ResponseField name="total_fee_vnd" type="number">Total fee in VND</ResponseField>
        <ResponseField name="fees" type="object">
          <Expandable title="AppliedFee">
            <ResponseField name="service_fee" type="number">Service fee in VND</ResponseField>
            <ResponseField name="min_fee" type="number">Minimum fee applied in VND</ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="pay_data" type="null">`null` for withdrawal orders</ResponseField>
        <ResponseField name="payment_info" type="object">
          Bank account the VND payout will be sent to
          <Expandable title="PaymentInfo">
            <ResponseField name="bank_id" type="integer">Bank ID</ResponseField>
            <ResponseField name="full_name" type="string">Account holder name</ResponseField>
            <ResponseField name="account_type" type="integer">`1` = Bank account number, `2` = ATM card number</ResponseField>
            <ResponseField name="account_number" type="string">Bank account number</ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="error" type="object">
          Populated when `state = 4` (Failed)
          <Expandable title="OrderError">
            <ResponseField name="error_code" type="string">Machine-readable error code</ResponseField>
            <ResponseField name="error_message" type="string">Human-readable error description</ResponseField>
            <ResponseField name="details" type="string">Additional error details</ResponseField>
            <ResponseField name="retryable" type="boolean">Whether the operation can be retried</ResponseField>
            <ResponseField name="source" type="integer">Internal error source identifier</ResponseField>
            <ResponseField name="timestamp" type="string">When the error occurred</ResponseField>
          </Expandable>
        </ResponseField>
        <ResponseField name="created_at" type="Timestamp">Order creation time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="updated_at" type="Timestamp">Last update time — `{ seconds, nanos }`</ResponseField>
        <ResponseField name="expired_at" type="Timestamp">Order expiry time — `{ seconds, nanos }`</ResponseField>
      </Expandable>
    </ResponseField>
  </Expandable>
</ResponseField>
